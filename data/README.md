# Runtime data

При первом запуске приложение создаст здесь `shop.db`.

Загруженные прайсы сохраняются в `imports/`, а извлечённые изображения — в `media/`.
Эти runtime-данные намеренно исключены из Git.
